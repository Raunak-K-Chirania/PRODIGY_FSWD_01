# PRODIGY_FSWD_01
Secure User Authentication: Implemented a robust authentication system using JWT (JSON Web Tokens) to ensure secure user login and session management. Passwords are encrypted before storage, protected routes restrict unauthorized access, and role-based authorization helps safeguard sensitive business operations and data.
